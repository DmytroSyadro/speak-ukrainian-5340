name: Speak-Ukrainian CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  BASE_URL: ${{ secrets.BASE_URL }}
  TEST_EMAIL: ${{ secrets.TEST_EMAIL }}
  TEST_PASSWORD: ${{ secrets.TEST_PASSWORD }}
  HEADLESS: true

permissions:
  contents: write
  pull-requests: write

jobs:
  lint-and-format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run Code Formatting Check
        run: npx prettier --check .

      - name: Run Linter
        run: npm run lint

  run-tests:
    needs: lint-and-format
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ matrix.browser }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-${{ matrix.browser }}-

      - name: Install Playwright browser & deps
        run: npx playwright install --with-deps ${{ matrix.browser }}

      - name: Run Playwright tests
        run: npx playwright test --project=${{ matrix.browser }}
        continue-on-error: true

      - name: Upload Allure Results
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-${{ matrix.browser }}
          path: allure-results
          retention-days: 1

  generate-report:
    needs: run-tests
    if: ${{ !cancelled() }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Allure history
        continue-on-error: true
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: gh-pages

      - name: Create Combined Results Directory
        run: mkdir combined-results

      - name: Download Chromium Results
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: allure-results-chromium
          path: combined-results

      - name: Download Firefox Results
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: allure-results-firefox
          path: combined-results

      - name: Download Webkit Results
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: allure-results-webkit
          path: combined-results

      - name: Generate Combined Allure Report
        uses: simple-elf/allure-report-action@master
        with:
          allure_results: combined-results
          gh_pages: gh-pages
          allure_report: allure-report
          allure_history: allure-history
          subfolder: ${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.number) || 'main' }}

      - name: Fix permissions
        run: sudo chmod -R 777 allure-history

      - name: Validate test results
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          SUMMARY_FILE="allure-history/pr-${{ github.event.number }}/widgets/summary.json"

          if [ ! -f "$SUMMARY_FILE" ]; then
            echo "⚠️ Summary file not found. Skipping."
            exit 0
          fi

          FAILED=$(jq '.statistic.failed' "$SUMMARY_FILE")
          BROKEN=$(jq '.statistic.broken' "$SUMMARY_FILE")

          echo "Total Failed: $FAILED"
          echo "Total Broken: $BROKEN"

          if [ "$FAILED" -gt 0 ] || [ "$BROKEN" -gt 0 ]; then
            echo "❌ Tests failed in one or more browsers"
            exit 1
          fi

          echo "✅ All tests passed across all browsers"

      - name: Clean archive assets from history
        if: ${{ always() }}
        run: |
          echo "🧹 Cleaning up: removing all archives from allure-history..."
          find allure-history -type f \( -name "*.zip" -o -name "*.tar.gz" -o -name "*.tgz" -o -name "*.rar" -o -name "*.7z" \) -exec rm -fv {} +
          echo "✅ Archive assets removed successfully."

      - name: Deploy Allure Report
        if: ${{ success() }}
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-history
          force_orphan: false
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'

      - name: Comment PR with report link
        if: ${{ success() && github.event_name == 'pull_request' }}
        uses: thollander/actions-comment-pull-request@v2
        with:
          comment_tag: allure-report-link
          message: |
            📊 **Allure Report for PR-${{ github.event.number }} (All Browsers)**

            🔗 "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/pr-${{ github.event.number }}/"
